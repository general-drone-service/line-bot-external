export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>LINE Bot — GDS 低空作業</h1>
      <p>Webhook endpoint: <code>POST /api/webhook</code></p>
    </main>
  )
}
